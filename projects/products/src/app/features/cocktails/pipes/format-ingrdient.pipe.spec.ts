import { FormatIngrdientPipe } from './format-ingrdient.pipe';

describe('FormatIngrdientPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatIngrdientPipe();
    expect(pipe).toBeTruthy();
  });
});
